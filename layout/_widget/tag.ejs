<% if (site.tags.length){ %>
    <div class="widget-wrap">
        <h4 class="widget-title"><%= __('tags') %></h4>
        <div class="widget">
            <%- list_tags({show_count: theme.show_count}) %>
        </div>
    </div>
<% } %>
